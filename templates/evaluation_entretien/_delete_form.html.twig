<form method="post" action="{{ path('app_evaluation_entretien_delete', {'id': evaluation_entretien.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ evaluation_entretien.id) }}">
    <button class="btn">Delete</button>
</form>
