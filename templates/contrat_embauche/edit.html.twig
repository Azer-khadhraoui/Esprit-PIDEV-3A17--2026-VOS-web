{% extends 'gestion_offre_base.html.twig' %}

{% block title %}Modifier un contrat | VOS{% endblock %}

{% block body %}
<div class="admin-shell">
    <aside class="sidebar">
        <div class="brand">
            <span class="brand-dot"></span>
            <span>VOS Admin</span>
        </div>
        <nav class="nav-list">
            <a class="nav-item" href="{{ path('recrutement_index') }}">Gestion Recrutements</a>
            <a class="nav-item" href="{{ path('app_admin_forum') }}">Forum</a>
            <a class="nav-item" href="{{ path('app_admin_reports') }}">Rapports</a>
        </nav>
        <div class="sidebar-footer">Panel Administration</div>
    </aside>

    <main class="content">
        <header class="topbar">
            <div>
                <h1>Modifier le contrat #{{ contrat.id }}</h1>
                <p>Mettez à jour les détails du contrat.</p>
            </div>
            <a href="{{ path('contrat_embauche_index') }}" class="btn-logout">Retour à la liste</a>
        </header>

        <section class="table-card">
            {{ include('contrat_embauche/_form.html.twig', {'form': form, 'button_label': 'Mettre à jour'}) }}
        </section>
    </main>
</div>
{% endblock %}
