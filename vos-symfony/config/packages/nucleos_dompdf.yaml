nucleos_dompdf:
    defaults:
        chroot: '%kernel.project_dir%/public' # See https://github.com/dompdf/dompdf/wiki/Usage#options
        logOutputFile: '' # Prevent creating `/tmp/log.htm` which causes problems in some server configurations
